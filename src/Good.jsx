import React from 'react'

function Good({ good_click }) {
  return (
    <div>
        {/* <button className='button' onClick={() => good_click()} >Good</button> */}
    </div>
  )
}

export default Good